/**
 * Tokens da casca escura (barra lateral, painel de edição, canvas). Os
 * cartões de nó (`NoBase.tsx`) não usam isso — eles ficam claros de
 * propósito, para se destacarem sobre o fundo escuro.
 */
export const CASCA = {
  fundo: "#14151b",
  fundoElevado: "#1e2029",
  borda: "#31333f",
  texto: "#e7e8ee",
  textoMuted: "#9295a6",
  destaque: "#25d366",
} as const;
